---
- name: Setup CI Server
  hosts: ci_servers
  become: true
  vars:
    workdir: /home/ec2-user/ci
    swapfile: /swapfile

  tasks:
    # -----------------------------
    # 1. TẠO SWAP (2GB) - Để Jenkins không bị crash do thiếu RAM
    # -----------------------------
    - name: Create 2GB swapfile
      shell: |
        fallocate -l 2G {{ swapfile }}
        chmod 600 {{ swapfile }}
        mkswap {{ swapfile }}
        swapon {{ swapfile }}
      args:
        creates: "{{ swapfile }}"

    # -----------------------------
    # 2. CÀI DOCKER
    # -----------------------------
    - name: Install Docker
      dnf:
        name: docker
        state: present

    - name: Start Docker service
      service:
        name: docker
        state: started
        enabled: yes

    - name: Add ec2-user to docker group
      user:
        name: ec2-user
        groups: docker
        append: yes

    # -----------------------------
    # 3. CÀI AWS CLI v2 (QUAN TRỌNG: Để login ECR)
    # -----------------------------
    - name: Install unzip
      dnf:
        name: unzip
        state: present
    
    - name: Download AWS CLI
      unarchive:
        src: "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip"
        dest: "/tmp"
        remote_src: yes
    
    - name: Install AWS CLI
      shell: /tmp/aws/install --update
      ignore_errors: yes

    # -----------------------------
    # 4. CÀI DOCKER COMPOSE PLUGIN
    # -----------------------------
    - name: Install Docker Compose plugin v2
      shell: |
        mkdir -p /usr/local/lib/docker/cli-plugins
        curl -SL https://github.com/docker/compose/releases/download/v2.25.0/docker-compose-linux-x86_64 \
          -o /usr/local/lib/docker/cli-plugins/docker-compose
        chmod +x /usr/local/lib/docker/cli-plugins/docker-compose
      args:
        creates: /usr/local/lib/docker/cli-plugins/docker-compose

    # -----------------------------
    # 5. CẤU HÌNH SYSCTL CHO SONARQUBE
    # -----------------------------
    - name: Set vm.max_map_count for SonarQube
      sysctl:
        name: vm.max_map_count
        value: '262144'
        state: present
        reload: yes

    # -----------------------------
    # 6. CHUẨN BỊ THƯ MỤC LÀM VIỆC
    # -----------------------------
    - name: Create CI working directory
      file:
        path: "{{ workdir }}"
        state: directory
        owner: ec2-user
        mode: '0755'

    - name: Copy Docker Compose file
      copy:
        src: ./files/docker-compose.yml
        dest: "{{ workdir }}/docker-compose.yml"
        owner: ec2-user

    # -----------------------------
    # 7. CHẠY JENKINS & SONARQUBE
    # -----------------------------
    - name: Start Jenkins first
      shell: docker compose up -d jenkins
      args:
        chdir: "{{ workdir }}"

    - name: Start SonarQube after Jenkins
      shell: docker compose up -d sonarqube
      args:
        chdir: "{{ workdir }}"

    # -----------------------------
    # 8. CÀI CÁC CÔNG CỤ BỔ TRỢ (HELM, TRIVY)
    # -----------------------------
    - name: Install Helm CLI
      shell: curl -fsSL https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
      args:
        creates: /usr/local/bin/helm

    - name: Install Trivy
      shell: |
        curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh -s -- -b /usr/local/bin
      args:
        creates: /usr/local/bin/trivy